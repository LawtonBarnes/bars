# Settings for the bars NTSC test pattern generator.
# Edit custom_text below, then press 'C' at the console to display it
# (no restart needed -- it's re-read from this file each time).
[bars]
custom_text = METAL SHOP
